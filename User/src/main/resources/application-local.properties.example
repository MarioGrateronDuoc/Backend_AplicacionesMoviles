# Local override example for `User`
# Copy this file to `application-local.properties` in the same folder and
# fill real credentials only on your machine (DO NOT commit `application-local.properties`).

spring.datasource.url=jdbc:mysql://caboose.proxy.rlwy.net:35580/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=CHANGE_ME
